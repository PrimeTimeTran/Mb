<!-- Form used for creating the various Messages on the Board -->
<%= simple_form_for @message do |f| %>
  <%= f.input :title, label: "Message Title" %>
  <%= f.input :description %>
  <%= f.button :submit %>
<% end %>
